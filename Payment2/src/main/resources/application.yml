spring:
  application:
    name: taskstudent
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: myuser
    password: 12345
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379

